#if false
    import Foundation

// Disabled: duplicate parser (kept for reference) — replaced by KickbasePlayerService's parsing to avoid transpiler collisions.

/*
This file previously contained a top-level parseMarketPlayersFromResponse helper used for debugging.
It is intentionally disabled to prevent duplicate symbol generation in transpilation.
*/

#endif
